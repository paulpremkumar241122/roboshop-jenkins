- name: Create Jenkins Jobs
  hosts: localhost
  connection: local
  vars:
    jobs:
      - roboshop-terraform
      - frontend
      - user
      - catalogue
      - cart
      - payment
      - shipping
  roles:
    - jenkins-jobs